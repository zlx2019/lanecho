org.gradle.jvmargs=-Xmx2g
org.gradle.caching=true
# AGP 9 defaults to built-in Kotlin + new DSL; the explicit KGP that :core
# shares needs the classic DSL (revisit when migrating to built-in Kotlin)
android.newDsl=false
