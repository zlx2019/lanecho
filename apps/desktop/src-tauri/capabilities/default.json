{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "窗口默认权限: 核心 IPC + 窗口隐藏(关窗进托盘 / 面板失焦隐)+ 主题切换",
  "windows": ["main", "panel"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-set-theme"
  ]
}
