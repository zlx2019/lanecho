{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default window permissions: core IPC + window hiding (minimize to tray when closed / hide panel when it loses focus) + theme switching + external links in the About window",
  "windows": ["main", "panel", "preview", "about"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-set-theme",
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "https://github.com/zlx2019/lanecho" }]
    }
  ]
}
